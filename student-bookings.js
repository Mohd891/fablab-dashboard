(function(){
  if(document.body.dataset.page!=='portal') return;
  const S='https://nwlqroyagrfuzfrtikln.supabase.co';
  const K='sb_publishable_AqI5cuM8pFcrc2UsiC-1zA_Sullikq7';
  let sb;
  const today=()=>{const d=new Date();return `${d.getFullYear()}-${String(d.getMonth()+1).padStart(2,'0')}-${String(d.getDate()).padStart(2,'0')}`};
  const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[c]));
  function student(){try{const u=typeof user==='function'?user():null,db=typeof loadDB==='function'?loadDB():null,s=u&&db?.students?.find(x=>String(x.userId)===String(u.id));return u&&s?{name:s.name||u.name,email:u.email,phone:s.phone||''}:null}catch{return null}}
  async function rpc(name,args){let r=await sb.rpc(name,args);if(!r.error)return r;try{const res=await fetch(`${S}/rest/v1/rpc/${name}`,{method:'POST',headers:{apikey:K,Authorization:`Bearer ${K}`,'Content-Type':'application/json'},body:JSON.stringify(args)});const text=await res.text();let data;try{data=JSON.parse(text)}catch{data=null}if(res.ok)return{data,error:null};return{data:null,error:{message:data?.message||data?.error||text||r.error.message,details:data?.details,hint:data?.hint}}}catch{return r}}
  function styles(){if(document.getElementById('studentBookingStyles'))return;const s=document.createElement('style');s.id='studentBookingStyles';s.textContent=`#studentBookingCard{margin:24px auto;max-width:1100px;background:#fff;border:1px solid #e6ebf2;border-radius:20px;padding:22px;box-shadow:0 10px 30px rgba(19,32,51,.07)}#studentBookingCard h2{margin:0 0 6px}.sb-muted{color:#718096}.sb-form{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:12px;margin-top:16px}.sb-form label{display:flex;flex-direction:column;gap:6px;font-weight:700}.sb-form input,.sb-form select,.sb-form textarea{font:inherit;border:1px solid #d8e0ea;border-radius:10px;padding:11px;background:#fff}.sb-form textarea{min-height:80px}.sb-full{grid-column:1/-1}.sb-msg{margin-top:10px;font-weight:700}.sb-table{width:100%;border-collapse:collapse;margin-top:16px;min-width:700px}.sb-table th,.sb-table td{padding:10px;border-bottom:1px solid #edf0f4;text-align:right}.sb-scroll{overflow-x:auto}.sb-badge{display:inline-flex;padding:5px 9px;border-radius:999px;font-size:12px;font-weight:800}.sb-pending{background:#fff7df;color:#8a5b00}.sb-approved{background:#e8f7ee;color:#137a42}.sb-rejected{background:#fff0f0;color:#b42318}@media(max-width:700px){.sb-form{grid-template-columns:1fr}.sb-full{grid-column:auto}#studentBookingCard{margin:18px 10px;padding:16px}}`;document.head.appendChild(s)}
  function inject(){if(document.getElementById('studentBookingCard'))return;styles();const side=document.querySelector('#portalSide,.portal-side,aside');if(side&&!side.querySelector('[data-student-booking-link]')){const a=document.createElement('a');a.href='#studentBookingCard';a.textContent='حجز موعد';a.dataset.studentBookingLink='1';side.appendChild(a);a.onclick=e=>{e.preventDefault();document.getElementById('studentBookingCard')?.scrollIntoView({behavior:'smooth'})}}
    const host=document.querySelector('main')||document.body,card=document.createElement('section');card.id='studentBookingCard';card.innerHTML=`<h2>📅 حجز موعد</h2><p class="sb-muted">اختر القسم واليوم والوقت، ثم أرسل طلبك. ستظهر حالة الطلب هنا بعد مراجعته.</p><form id="studentBookingForm" class="sb-form"><label>القسم<select name="department" required><option value="">اختر القسم</option><option>القص بالليزر</option><option>الروبوتات</option><option>الإلكترونيات</option><option>الطباعة ثلاثية الأبعاد</option><option>الخشب</option></select></label><label>التاريخ<input type="date" name="date" required></label><label>الوقت<input type="time" name="time" required></label><label class="sb-full">ملاحظات (اختياري)<textarea name="notes" maxlength="1000" placeholder="اكتب سبب الموعد أو أي ملاحظة..."></textarea></label><div class="sb-full"><button type="submit" class="btn primary" id="studentBookingSubmit">إرسال طلب الحجز</button><div id="studentBookingMsg" class="sb-msg"></div></div></form><div id="studentBookingsList"></div>`;host.appendChild(card);card.querySelector('[name=date]').min=today();card.querySelector('#studentBookingForm').addEventListener('submit',submit);loadBookings()}
  async function submit(e){e.preventDefault();const st=student(),f=e.currentTarget,msg=document.getElementById('studentBookingMsg'),btn=document.getElementById('studentBookingSubmit');if(!st){msg.textContent='تعذر قراءة بيانات حساب الطالب. سجّل الدخول مرة أخرى.';msg.style.color='#b42318';return}const fd=new FormData(f),department=String(fd.get('department')||''),date=String(fd.get('date')||''),time=String(fd.get('time')||''),notes=String(fd.get('notes')||'');if(!department||!date||!time){msg.textContent='فضلاً اختر القسم والتاريخ والوقت.';msg.style.color='#b42318';return}if(date<today()){msg.textContent='لا يمكن اختيار تاريخ سابق.';msg.style.color='#b42318';return}btn.disabled=true;msg.textContent='جاري إرسال طلب الحجز...';msg.style.color='';const payload={p_student_name:String(st.name||'').trim(),p_student_email:String(st.email||'').trim(),p_student_phone:String(st.phone||'').trim(),p_department:department,p_appointment_date:date,p_appointment_time:time,p_notes:notes};const r=await rpc('create_student_appointment',payload);if(r.error){const raw=[r.error.message,r.error.details,r.error.hint].filter(Boolean).join(' — ');msg.textContent=raw.includes('محجوز')?'هذا الموعد محجوز بالفعل لهذا القسم. اختر وقتًا آخر.':raw||'تعذر إرسال طلب الحجز.';msg.title=raw;msg.style.color='#b42318';btn.disabled=false;return}const a=Array.isArray(r.data)?r.data[0]:r.data;try{await fetch(`${S}/functions/v1/send-appointment-email`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({student_name:a?.student_name||st.name,student_email:a?.student_email||st.email,student_phone:a?.student_phone||st.phone,department:a?.department||department,appointment_date:a?.appointment_date||date,appointment_time:String(a?.appointment_time||time).slice(0,5),notes:a?.notes||notes})})}catch{}f.reset();f.querySelector('[name=date]').min=today();msg.textContent='تم إرسال طلب الحجز بنجاح. بانتظار موافقة الإدارة.';msg.style.color='#137a42';btn.disabled=false;loadBookings()}
  async function loadBookings(){const st=student(),el=document.getElementById('studentBookingsList');if(!st||!el)return;const r=await rpc('get_student_appointments',{p_email:st.email,p_phone:st.phone});if(r.error){el.innerHTML='<p class="sb-muted">تعذر تحميل حالة الحجوزات حاليًا.</p>';return}const data=r.data;if(!data?.length){el.innerHTML='<div style="margin-top:18px" class="sb-muted">لا توجد حجوزات حتى الآن.</div>';return}const label=s=>s==='approved'?'تمت الموافقة':s==='rejected'?'تم الرفض':'بانتظار المراجعة',cls=s=>s==='approved'?'sb-approved':s==='rejected'?'sb-rejected':'sb-pending';el.innerHTML=`<h3 style="margin:24px 0 8px">حجوزاتي</h3><div class="sb-scroll"><table class="sb-table"><thead><tr><th>القسم</th><th>التاريخ</th><th>الوقت</th><th>الحالة</th><th>ملاحظة الإدارة</th></tr></thead><tbody>${data.map(a=>`<tr><td>${esc(a.department)}</td><td>${esc(a.appointment_date)}</td><td>${esc(String(a.appointment_time||'').slice(0,5))}</td><td><span class="sb-badge ${cls(a.status)}">${label(a.status)}</span></td><td>${esc(a.admin_note||'—')}</td></tr>`).join('')}</tbody></table></div>`}
  function init(){if(!window.supabase?.createClient)return setTimeout(init,250);sb=window.supabase.createClient(S,K);inject()}
  if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',init,{once:true});else setTimeout(init,0);
})();